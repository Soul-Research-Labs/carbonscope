apiVersion: v1
kind: ResourceQuota
metadata:
  name: carbonscope-quota
  namespace: carbonscope
spec:
  hard:
    requests.cpu: "8"
    requests.memory: 8Gi
    limits.cpu: "16"
    limits.memory: 16Gi
    pods: "30"
    persistentvolumeclaims: "5"
