feat: webhook HTTP dispatch with delivery logging

- Add WebhookDelivery model for tracking delivery attempts
- Replace stub dispatch with real httpx HTTP POST requests
- Sign payloads with HMAC-SHA256 (X-CarbonScope-Signature header)
- Log status_code, response_body, duration_ms, success/error per delivery
- Add GET /webhooks/{id}/deliveries endpoint with pagination
- Wire dispatch_event into data upload and estimate endpoints
- Add WebhookDeliveryOut schema
- All 167 tests pass
