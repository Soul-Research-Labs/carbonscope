feat: report export endpoint (CSV and JSON)

- Add GET /reports/export with format=csv|json and optional year filter
- Route placed before /reports/{report_id} to avoid path conflict
- CSV output with proper headers and Content-Disposition
- JSON export with indented formatting
- Respects soft-delete filter
- 3 new tests for export (170 tests pass)
