feat: Docker and production configuration

- Add multi-stage Dockerfile (backend + frontend targets)
- Add docker-compose.yml with health checks and volume mounts
- Enable Next.js standalone output for containerized deployment
- Make backend URL configurable via BACKEND_URL env var
- Add .dockerignore for lean builds
- Add .env.example with all configurable env vars
- All 170 tests pass
